# not all of these are strictly necessary. i just dumped my entire environment here
name: cheff
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - pillow=10.4.0
  - pip:
      - torch==2.0.1
      - torchvision==0.15.2
      - fastai==2.7.12
      - matplotlib==3.9.2
